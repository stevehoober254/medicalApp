import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.medicalapp.com',
  appName: 'medicalApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
